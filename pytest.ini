[pytest]
testpaths = tests
cache_dir = tests/.artifacts/.pytest_cache
norecursedirs = archive dist venv .git tests/.artifacts .pytest_cache .ruff_cache scripts

markers =
    live: marks tests that require a live local llama-server running
    slow: slow live-model tests (only run when explicitly requested)
