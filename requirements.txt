# Stet v1.0 — Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework (Qt 6)
PyQt6>=6.11.0

# Clipboard access
pyperclip>=1.11.0

# HTTP client (llama.cpp OpenAI-compatible API + streaming)
requests>=2.34.2

# Offline GGUF metadata reading (model info, reasoning detection)
gguf==0.19.0

# ── Build / release only (uncomment when building) ──────────────────────────
pyinstaller>=6.0

# ── macOS native integration ────────────────────────────────────────────────
# Installed only when running or building on macOS.
pyobjc-core>=10.3; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.3; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.3; sys_platform == "darwin"
pyobjc-framework-ServiceManagement>=10.3; sys_platform == "darwin"

